# -*- lexical-binding: t; -*-

#+TITLE: Emacs Config (Modular)
#+AUTHOR: Nathan Martins Cunha
#+PROPERTY: header-args:emacs-lisp :tangle yes :results silent


#+begin_src emacs-lisp
;;; config.el --- Modular Configuration -*- lexical-binding: t -*-
#+end_src
# Keep modules visible to Nix use-package parsing.
#+include: "core.org"
#+include: "ui.org"
#+include: "theme.org"
#+include: "completion.org"
#+include: "tools.org"
#+include: "bindings.org"
#+include: "notes.org"
#+include: "containers.org"
#+include: "ai.org"
#+include: "eca.org"

#+begin_src emacs-lisp
(setq custom-file (expand-file-name "custom.el" user-emacs-directory))
(when (file-exists-p custom-file)
  (load custom-file 'noerror))
(dolist (file '("core.el" "ui.el" "theme.el" "completion.el" "tools.el" "bindings.el" "notes.el" "containers.el" "ai.el" "eca.el"))
  (let ((path (expand-file-name file user-emacs-directory)))
    (when (file-exists-p path)
      (load path 'noerror 'nomessage))))
#+end_src
#+begin_src bash

#+end_src
